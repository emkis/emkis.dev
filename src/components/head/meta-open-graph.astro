---
import { metadata } from '@/metadata'

interface BaseProps {
  title: string
  description: string
}

interface WebsiteProps extends BaseProps {
  type: 'website'
}

interface ArticleProps extends BaseProps {
  type: 'article'
  publishedAt: string
  updatedAt: string
}

type Props = WebsiteProps | ArticleProps

const props = Astro.props
const canonicalUrl = new URL(Astro.url.pathname, Astro.site)
const isArticle = props.type === 'article'
---

<meta property="og:site_name" content={metadata.title} />
<meta property="og:title" content={props.title} />
<meta property="og:description" content={props.description} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:type" content={props.type} />
{isArticle && <meta property="article:published_time" content={new Date(props.publishedAt).toISOString()} />}
{isArticle && <meta property="article:modified_time" content={new Date(props.updatedAt).toISOString()} />}
<meta name="twitter:card" content="summary_large_image">
