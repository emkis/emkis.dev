---
const plausibleDomain = new URL(Astro.url.hostname, Astro.site).hostname
const isProduction = import.meta.env.PROD
---

{isProduction && (
  <script
    is:inline
    async
    data-domain={plausibleDomain}
    data-api="/api/event"
    src="/js/script.js"
  />
)}

  